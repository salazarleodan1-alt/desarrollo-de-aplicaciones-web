// Obtener elementos del DOM const formulario = document.getElementById("formProducto"); const lista = document.getElementById("listaProductos"); // Evento al enviar el formulario formulario.addEventListener("submit", function(evento){ // Evita que la página se recargue evento.preventDefault(); // Obtener valores del formulario let nombre = document.getElementById("nombreProducto").value; let precio = document.getElementById("precioProducto").value; // Crear un nuevo elemento de lista let nuevoProducto = document.createElement("li"); // Agregar clases de Bootstrap nuevoProducto.className = "list-group-item"; // Agregar contenido nuevoProducto.innerHTML = `<strong>${nombre}</strong> - Precio: $${precio}`; // Mostrar en pantalla lista.appendChild(nuevoProducto); // Limpiar formulario formulario.reset(); });